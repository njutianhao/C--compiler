int fact(int n) 
{
    if (n == 1)
        return n;
    else
        return (n * fact(n - 1)); 
}
int main()
{
    int m, result;
    int a[2];
    m = read();
    if (m > 1)
        result = fact(m);
    else
    result = 1;
    write(result);
    a[1]=1+2;
    a[0]=a[1];
    return 0;
}